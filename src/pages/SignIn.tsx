import { SignInForm } from "../components/modules/Authentication/SignInForm";

const SignIn = () => {
  return (
    <div className="container mx-auto flex items-center justify-center py-5 h-screen ">
      <SignInForm />
    </div>
  );
};

export default SignIn;
